#include "../src/RuleVariableComputer.h"
#include <iostream>
#include <vector>
#include <cstdlib>

typedef std::vector<unsigned int> ValueVector;
typedef std::vector<ValueVector*> TestVector;

bool RuleVariableComputer_test(void);
int main(void);
